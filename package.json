{
  "name": "pi-prune-chunks",
  "version": "0.1.0",
  "description": "Pi extension that parks bulky tool output behind restorable context cards",
  "main": "index.ts",
  "files": [
    "index.ts",
    "src/**/*.ts",
    "docs/**/*.md",
    "README.md",
    "LICENSE",
    "pi.d.ts"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:test": "rm -rf .tmp-test && tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test .tmp-test/test/*.test.js",
    "test:coverage": "npm run build:test && NODE_V8_COVERAGE=coverage node --test --experimental-test-coverage .tmp-test/test/*.test.js",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "biome check . && npm run typecheck && npm run test:coverage",
    "bench:context-savings": "rm -rf .tmp-bench && tsc -p bench/tsconfig.json && node .tmp-bench/bench/context-savings.js",
    "bench:durable-store": "rm -rf .tmp-bench && tsc -p bench/tsconfig.json && node .tmp-bench/bench/durable-store-value.js",
    "bench:policy-compare": "rm -rf .tmp-bench && tsc -p bench/tsconfig.json && node .tmp-bench/bench/policy-compare.js",
    "bench:e2e-pruning": "rm -rf .tmp-bench && tsc -p bench/tsconfig.json && node .tmp-bench/bench/e2e-pruning-impact.js",
    "pack:dry": "npm pack --dry-run"
  },
  "keywords": [
    "pi",
    "pi-coding-agent",
    "pi-extension",
    "context-management",
    "pruning",
    "reamer"
  ],
  "author": "cawalch",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "typebox": "^1.1.38"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.14",
    "@types/node": "^24.10.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": "22.x || 24.x"
  },
  "packageManager": "npm@11.6.2"
}
